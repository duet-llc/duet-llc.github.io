on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: codeberg-tiny-lazy
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: install lftp
        run: sudo apt-get update && sudo apt-get install lftp
      - name: setup dart
        uses: https://github.com/dart-lang/setup-dart@v1
      - name: install dependencies
        run: dart pub get
      - name: build
        run: dart run jaspr_cli:jaspr build --verbose
      - name: deploy
        run: |
          echo ${{ secrets.SFTP_HOST_KEYS }} >> $HOME/.ssh/known_hosts
          lftp -c "open ${{ secrets.SFTP_URL }} && mirror -Rev build/jaspr /"
