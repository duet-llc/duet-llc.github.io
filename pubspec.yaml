name: duet_llc_website
description: Website for Duet LLC
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  jaspr: ^0.22.4
  jaspr_content: ^0.5.1

dev_dependencies:
  build_runner: ^2.8.0
  build_web_compilers: ^4.2.3
  jaspr_builder: ^0.22.4
  jaspr_lints: ^0.6.1
  lints: ^6.0.0

jaspr:
  mode: static

